{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ template "kyverno-authz-server-control-plane.name" . }}
  labels:
    {{- include "kyverno-authz-server-control-plane.labels" . | nindent 4 }}
rules:
- apiGroups:
  - policies.kyverno.io
  resources:
  - validatingpolicies
  verbs:
  - get
  - list
  - watch
{{- end -}}
