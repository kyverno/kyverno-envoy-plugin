apiVersion: v1
kind: Service
metadata:
  name: {{ include "kyverno-envoy-plugin.fullname" . }}
  labels:
    {{- include "kyverno-envoy-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kyverno-envoy-plugin.selectorLabels" . | nindent 4 }}
