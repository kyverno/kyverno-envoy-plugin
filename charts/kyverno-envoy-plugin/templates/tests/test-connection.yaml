apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kyverno-envoy-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "kyverno-envoy-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kyverno-envoy-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
