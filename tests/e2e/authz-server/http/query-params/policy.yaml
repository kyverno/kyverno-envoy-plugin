# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/playground/refs/heads/main/schemas/json/v3/validatingpolicy-policies.kyverno.io-v1alpha1.json
apiVersion: policies.kyverno.io/v1alpha1
kind: ValidatingPolicy
metadata:
  name: policy
spec:
  evaluation:
    mode: HTTP
  validations:
  - expression: >
      object.queryParams.Get("foo") == "bar"  
        ?  http.Response(200)
        :  http.Response(403).WithBody("the needed query parameter was missing")
